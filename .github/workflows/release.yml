name: Release plugin-bigcommerce

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No internal URLs
        run: bash ./scripts/check-no-internal-urls.sh ./
      - name: Build zip
        run: |
          cd .
          zip -r ../bigcommerce.zip bigcommerce -x '*/node_modules/*' '*/vendor/*' '*/.git/*'
      - name: GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: bigcommerce.zip
          generate_release_notes: true
          name: BigCommerce ${{ github.ref_name }}
